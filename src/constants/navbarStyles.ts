import type { ModalStyleConfig } from "@/interfaces/modalStyles";

const authModalStyles: ModalStyleConfig = {
  overlay: {
    backgroundColor: "rgb(76, 76, 76)",
  },
  wrapper: { width: "50%" },
  title: { fontSize: "1.8rem" },
};

export default authModalStyles;
